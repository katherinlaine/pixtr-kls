<div>
  <%= link_to "Create a Group", new_group_path %>
</div>

<ul>
<% @groups.each do |group| %>
  <li><%= link_to group.name, group_path(group) %></li>
<% end %>
<ul>

<h4><%= link_to "Go back to Homepage", root_path %></h4>

 
